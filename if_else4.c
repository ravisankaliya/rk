#include<stdio.h>
void main()
// write a programe to find out wether the given number is singlr digit or double digit 

{
    int number = 0;

printf("enter value of number ");
scanf("%d",&number);

if(number<-9,number<9)
{
    printf("the nuber is single digit");
}
else
{
    printf("the number is double digit");
}
}